<!-- views/res_partner_views.xml -->
<odoo>
  <record id="view_partner_form_split_name" model="ir.ui.view">
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.view_partner_form"/>
    <field name="arch" type="xml">

      <!-- 1️⃣ Nombre separado -->
      <xpath expr="//field[@name='name']" position="before">
        <group string="Nombre">
            <field name="first_name" placeholder="Nombre"/>
            <div class="o_row">
                <div class="o_col-6">
                    <field name="last_name_father" placeholder="Apellido Paterno"/>
                </div>
                <div class="o_col-6">
                    <field name="last_name_mother" placeholder="Apellido Materno"/>
                </div>
            </div>
        </group>
      </xpath>

      <!-- 1.1 Ocultar el campo  -->
      <xpath expr="//field[@name='name']" position="attributes">
        <attribute name="invisible">1</attribute>
      </xpath>
      <xpath expr="//field[@name='function']" position="attributes">
        <attribute name="invisible">1</attribute>
      </xpath>
      <xpath expr="//field[@name='website']" position="attributes">
          <attribute name="invisible">1</attribute>
      </xpath>
      <xpath expr="//field[@name='title']" position="attributes">
          <attribute name="invisible">1</attribute>
      </xpath>
      <xpath expr="//field[@name='category_id']" position="attributes">
          <attribute name="invisible">1</attribute>
      </xpath>
      <xpath expr="//field[@name='name']" position="replace"/>
      <!-- <xpath expr="//header//field[@name='name']" position="replace"/> -->
      <xpath expr="//sheet//group/field[@name='name']" position="replace"/>

      <!-- 2️⃣ NUEVOS GRUPOS -->
      <xpath expr="//sheet/group" position="after">
        <!-- ── Antecedentes del Cliente ── -->
        <group string="Antecedentes del Cliente" colspan="4">
          <field name="birthdate_date"/>
          <field name="nationality_id"/>
          <field name="gender"/>
          <field name="marital_status"/>
          <field name="marital_regime"/>
        </group>

        <!-- ── Antecedentes de Actividad ── -->
        <group string="Antecedentes de Actividad" colspan="4">
          <field name="worker_type"/>
          <field name="amicar_activity_type_id"/>
          <field name="employment_date"/>
        </group>
      </xpath>

      <!-- 3️⃣ PESTAÑA “Documentos” -->
      <xpath expr="//page[@name='internal_notes']" position="after">
        <page string="Documentos" name="documents">
          <field name="document_links"
                 nolabel="1"
                 readonly="1"
                 widget="html"/>
        </page>
      </xpath>

    </field>
  </record>
</odoo>
